import { ArticleRootPath } from "@/types/articles";

/**
 * Base path for the blog page
 */
export const PATH_BLOG: ArticleRootPath = "/blog";

/**
 * Base path for the news page
 */
export const PATH_NEWS: ArticleRootPath = "/news";

/**
 * Fallback image for articles
 */
export const fallbackImage = "/images/aqa-article-img.webp";

/**
 * Item limits for the article list and article cards
 */
export const itemLimits = [3, 4, 6, 12];
