"use client";

import ErrorComponent from "@/components/error";

const Error = () => {
  return (
    <html>
      <body>
        <ErrorComponent />
      </body>
    </html>
  );
};

export default Error;
